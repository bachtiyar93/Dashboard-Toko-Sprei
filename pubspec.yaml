name: tnb_srm
description: Sweet Room Customer Order
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_messaging: ^7.0.0
  shared_preferences: ^0.5.10
  custom_progress_dialog: ^1.0.3
  http: ^0.12.0+2
  cupertino_icons: ^0.1.3
  crypto: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

