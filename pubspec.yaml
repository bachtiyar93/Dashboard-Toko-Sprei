name: tnb_srm
description: Sweet Room Customer Order
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_messaging: ^7.0.0
  path_provider: ^1.6.14
  shared_preferences: ^0.5.10
  workmanager: ^0.2.3

  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

